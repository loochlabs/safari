/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.mygdx.environments.EnvSlum;

/**
 *
 * @author looch
 */
public class SlumSection {/*extends EnvSection{
    
    
    public SlumSection(Vector2 pos, float width, float height, EnvSlum env, Coordinate coord){
        super(pos,width,height,env,coord);
        
        bg = MainGame.am.get(ResourceManager.SLUM_SECTION_BG);
    }
    
    @Override
    public void init(){
        
        //north wall
        if(sideTypes[0] == WALL){
            //solid wall
            //env.spawnEntity(new NullWall(new Vector2(pos.x + width/2, pos.y + height*0.10f + height ),  width/2,  border));//north
        }
       
        //east wall
        if(sideTypes[1] == WALL){
            //solid wall
            //env.spawnEntity(new NullWall(new Vector2(pos.x + width , pos.y + height*0.15f + height/2), border, height/2));
        }
        
        //south wall
        if(sideTypes[2] == WALL){
            //solid wall
            //env.spawnEntity(new NullWall(new Vector2(pos.x + width/2, pos.y + height*0.15f), width/2,  border));
        }
        
        
        //west wall
        if(sideTypes[3] == WALL){
            //solid wall
            //env.spawnEntity(new NullWall(new Vector2(pos.x , pos.y + height*0.15f + height/2), border, height/2));//west
        }
        
    }
    
    */
    
}
